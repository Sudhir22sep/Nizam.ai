const exchangeRates = { USD: 1, INR: 95.21, AED: 3.67, SAR: 3.73 }; const supportedCurrencies = ["USD", "INR", "AED", "SAR"]; const cartTotalUsd = 15; const inrRate = exchangeRates["INR"]; ["USD", "INR", "AED", "SAR"].forEach(c => { const fr = exchangeRates[c]; const t = cartTotalUsd * fr; const fc = supportedCurrencies.includes(c) ? c : "USD"; const bfr = exchangeRates[fc]; const tusd = t / bfr; const tinr = tusd * inrRate; const ap = Math.round(tinr * 100); console.log(c + " -> Razorpay: " + (ap/100).toFixed(2) + " OK: " + (ap === 142815)); });
